import React from "react";
import Header from "./components/Header/Header";

const Home = ({ productItems, addToCart, cartItems, shopItems }) => {
  return (
    <>
      <Header cartItems={cartItems} />
    
    </>
  );
};

export default Home;
